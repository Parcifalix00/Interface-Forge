interface:
  display_name: "Frontend Visual QA"
  short_description: "Verify UI with visual evidence and a device matrix"
  default_prompt: "Use $frontend-visual-qa for the requested task, respect the project, and report only checks actually performed."
policy:
  allow_implicit_invocation: true
